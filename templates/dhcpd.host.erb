host <%= @host %> {
  hardware ethernet   <%= @mac %>;
  fixed-address       <%= @ip %>;
  ddns-hostname       "<%= @name %>";
  <% if has_variable?( 'hostname' ) -%>
  option host-name    "<%= @name %>.<%= @dnsdomain.first %>";
  <% end -%>
}
